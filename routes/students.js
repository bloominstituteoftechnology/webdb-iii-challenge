const express = require('express');
const router = express.Router();

/* ---------- GET /api/students---------- */

/* ---------- GET /api/students/:id ---------- */

/* ---------- POST /api/students ---------- */

/* ---------- PUT /api/students/:id ---------- */

/* ---------- DELETE /api/students/:id ---------- */